simple-stegonography
====================

This class can hide and retrieve text in PNG images. It uses a really simple algorithm to do this: changes every X ($steps) pixel's RGB value to the text's ASCII values
